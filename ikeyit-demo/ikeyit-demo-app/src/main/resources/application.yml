# 应用配置
spring:
  application:
    name: ikeyit-demo

# 服务器配置
server:
  port: 8080

# 日志配置
logging:
  level:
    root: INFO
    com.ikeyit: DEBUG

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always